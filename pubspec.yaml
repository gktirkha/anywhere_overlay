name: anywhere_overlay
description: "A Plugin To Show Overlay Anywhere"
version: 1.0.2
repository: https://github.com/gktirkha/anywhere_overlay

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter:
